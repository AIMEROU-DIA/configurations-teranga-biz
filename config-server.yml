server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/AIMEROU-DIA/Configurations-teranga-biz
          default-label: main
          clone-on-start: true

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
